---
// Hero.astro
---

<section class="hero">
  <div class="hero-logo">
    <img src="/image/logo.png" alt="Logo Barbería" class="logo-image" />
  </div>
  
  <div class="hero-content">
    <div class="hero-text">
      <h1 class="hero-title">
        Agenda tu cita de barbería de forma fácil y rápida
      </h1>
      <p class="hero-subtitle">
        Simplifica el proceso de agendamiento de citas de barbería. 
        Reserva tu cita en minutos, sin complicaciones.
      </p>
      <div class="hero-buttons">
        <a href="#citas" class="btn btn-primary">Agendar Cita</a>
        <a href="#servicios" class="btn btn-secondary">Ver Servicios</a>
      </div>
    </div>
    
    <div class="hero-image">
      <img src="/image/logo.png" alt="Logo Barbería" class="hero-logo-image" />
    </div>
  </div>
  
  <div class="hero-stats">
    <div class="stat">
      <div class="stat-number">1000+</div>
      <div class="stat-label">Citas Agendadas</div>
    </div>
    <div class="stat">
      <div class="stat-number">10+</div>
      <div class="stat-label">Barberos Profesionales</div>
    </div>
    <div class="stat">
      <div class="stat-number">24/7</div>
      <div class="stat-label">Soporte Disponible</div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: linear-gradient(135deg, #000000 0%, #1a1a1a 100%);
    color: white;
    padding: 4rem 0;
    min-height: 80vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }
  
  .hero-logo {
    text-align: center;
    margin-bottom: 2rem;
  }
  
  .logo-image {
    width: 120px;
    height: auto;
    filter: brightness(0) invert(1);
    opacity: 0.9;
  }
  
  .hero-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  
  .hero-title {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }
  
  .hero-subtitle {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    line-height: 1.6;
    opacity: 0.9;
  }
  
  .hero-buttons {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }
  
  .btn {
    padding: 1rem 2rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 1.1rem;
    transition: all 0.3s;
    display: inline-block;
  }
  
  .btn-primary {
    background: #95D600;
    color: #000000;
  }
  
  .btn-primary:hover {
    background: #7ab800;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(149, 214, 0, 0.3);
  }
  
  .btn-secondary {
    background: transparent;
    color: white;
    border: 2px solid #95D600;
  }
  
  .btn-secondary:hover {
    background: #95D600;
    color: #000000;
    transform: translateY(-2px);
  }
  
  .hero-image {
    display: flex;
    justify-content: center;
    align-items: center;
  }
  
  .hero-logo-image {
    width: 300px;
    height: auto;
    filter: brightness(0) invert(1);
    opacity: 0.8;
    animation: float 3s ease-in-out infinite;
  }
  
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-20px);
    }
  }
  
  .hero-stats {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    margin-top: 4rem;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
  }
  
  .stat {
    text-align: center;
    padding: 2rem;
    background: rgba(149, 214, 0, 0.1);
    border-radius: 12px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(149, 214, 0, 0.2);
  }
  
  .stat-number {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: #95D600;
  }
  
  .stat-label {
    font-size: 1rem;
    opacity: 0.9;
  }
  
  @media (max-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 2rem;
    }
    
    .hero-title {
      font-size: 2rem;
    }
    
    .hero-buttons {
      justify-content: center;
    }
    
    .hero-logo-image {
      width: 200px;
    }
    
    .logo-image {
      width: 80px;
    }
    
    .hero-stats {
      grid-template-columns: 1fr;
    }
  }
</style> 